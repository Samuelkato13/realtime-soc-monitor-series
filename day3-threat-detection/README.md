# Day 3 – Threat Detection System

This stage introduces threat detection logic.

The system analyzes logs in real time and detects suspicious login patterns.

Detection Rule:

If an IP address generates more than 5 failed login attempts, an alert is triggered.

This simulates brute force detection used in SOC monitoring systems.
